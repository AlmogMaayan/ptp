---
description: Set up local ptp telemetry, creating the store and writing the receiver configuration
argument-hint: "(no arguments)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-telemetry-setup` skill (`skills/ptp-telemetry-setup/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
