---
description: Report the installed ptp plugin version and where it was loaded from
argument-hint: "(no arguments — read-only)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-version` skill (`skills/ptp-version/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
