---
description: Start the local ptp telemetry receiver so that later runs are recorded
argument-hint: "(no arguments)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-telemetry-start` skill (`skills/ptp-telemetry-start/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
