---
description: Return to a clean, up-to-date master, switching and pulling only when the tree is clean
argument-hint: "(no arguments — switches to master and pulls when the tree is clean)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-master` skill (`skills/ptp-master/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
