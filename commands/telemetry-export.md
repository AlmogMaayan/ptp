---
description: Export recorded ptp telemetry from the local store into a portable file
argument-hint: "(no flag, no argument, no selector — export is global and re-derives every spans.csv)"
---

## Arguments

This command takes no arguments. The export is global.

## Owner

Invoke the `ptp-telemetry-export` skill (`skills/ptp-telemetry-export/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
