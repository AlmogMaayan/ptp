---
description: Report whether the local ptp telemetry receiver is running and where its store lives
argument-hint: "(no arguments)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-telemetry-status` skill (`skills/ptp-telemetry-status/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
