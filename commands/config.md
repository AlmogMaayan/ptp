---
description: Set one ptp configuration value interactively, writing it into the chosen user or project config
argument-hint: "(no arguments — fully interactive)"
---

## Arguments

This command takes no arguments. It is fully interactive.

## Owner

Invoke the `ptp-config` skill (`skills/ptp-config/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
