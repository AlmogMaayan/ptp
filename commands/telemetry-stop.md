---
description: Stop the local ptp telemetry receiver and leave the recorded store in place
argument-hint: "(takes no argument)"
---

## Arguments

This command takes no arguments.

## Owner

Invoke the `ptp-telemetry-stop` skill (`skills/ptp-telemetry-stop/SKILL.md`).

## Report

Report the change id where the command resolved one, the resulting state, any failures, and the next command to run.
